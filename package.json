{
  "name": "vue-icon-builder",
  "version": "0.1.0",
  "description": "automatically generates vue components from svg icons",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "quarkpunk",
  "license": "ISC",
  "dependencies": {
    "fs": "^0.0.1-security",
    "path": "^0.12.7"
  },
  "devDependencies": {
    "cheerio": "^1.0.0"
  }
}
